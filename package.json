{
  "name": "effect-ive-js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @on-the-ground/effect-core build && yarn workspace @on-the-ground/effect-raise build && yarn workspace @on-the-ground/tableizable build",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
